const age = 
const limit = 18

if (age >= limit) {
  console.log(`you are eligible for the licence`)
  
  
  
}
else {diff =  limit - age
      console.log(`you have to wait ${diff} to get a licence`)
     
     }
